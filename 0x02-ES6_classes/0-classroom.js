// module that implement classRoom class.

export default class ClassRoom {
  /* create class
  *  It accept one attribute name maxStudentsSize, maximum no of student in the class.
  */
  constructor(maxStudentsSize) {
    this._maxStudentsSize = maxStudentsSize;
  }
}
